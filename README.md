# flash-attention
building flash attention from scratch

done: attention, multi-head attention, and flash attention references in PyTorch, multi-tile + multi-block flash attention kernel \
building: causal masking in FA kernel
